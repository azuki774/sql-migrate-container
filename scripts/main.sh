#!/bin/bash
BIN=/usr/local/bin/sql-migrate

GIT_REPO=${GIT_REPO}
MIGRATE_DIR=${MIGRATE_DIR}
MIGRATE_ENV=${MIGRATE_ENV}

echo "migrate start"
echo "Git Repository: ${GIT_REPO}"
echo "migrate directory: ${MIGRATE_DIR}"
echo "migrate env: ${MIGRATE_ENV}"
